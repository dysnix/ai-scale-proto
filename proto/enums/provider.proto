syntax = "proto3";

package enums;

option go_package = "github.com/dysnix/ai-proto/external/proto/enums";

enum PeriodType {
  Daily = 0;
  Weekly = 1;
  Monthly = 2;
}

enum MetricsType {
  Cpu = 0;
  Memory = 1;
  Disk = 2;
  Network = 3;
  Logs = 4;
}

enum ResourceType {
  Deployment = 0;
  StatefulSet = 1;
  DaemonSet = 2;
}